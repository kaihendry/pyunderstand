[project]
name = "pyunderstand"
version = "0.1.15"
description = "Document a dependency"
readme = "README.md"
requires-python = ">=3.9"
authors = [
    { name = "Kai Hendry", email = "hendry@iki.fi" }
]

dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

