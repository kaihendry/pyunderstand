[project]
name = "pyunderstand"
version = "v0.1.4"
description = "Document a dependency"
readme = "README.md"
requires-python = ">=3.9"
authors = [
    { name = "Kai Hendry", email = "hendry@iki.fi" }
]

dependencies = []
[dependency-groups]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
